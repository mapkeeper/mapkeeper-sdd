# PostgreSQL runs on a separate Proxmox LXC; no DB container is added to Compose.
# The async driver is required: mapkeeper.core.config rejects any other scheme.
DATABASE_URL=postgresql+asyncpg://mapkeeper:change-me@10.0.0.10:5432/mapkeeper

# MVP has no login. Approvals are attributed to this fixed UUID, never to a request field.
MVP_ACTOR_ID=00000000-0000-4000-8000-000000000000

# Log every SQL statement. Keep off outside local debugging so no query text leaks.
DB_ECHO=false

# Optional. Without it the SEO generation falls back to a deterministic stub, so
# the UC2 flow runs offline. Set it to call Gemini for real.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
GEMINI_TIMEOUT_SECONDS=20.0

# Separate empty database used by tests/integration. Those tests are skipped when
# this is unset. Never point it at a database holding real data - it is migrated
# and written to.
TEST_DATABASE_URL=postgresql+asyncpg://mapkeeper:change-me@10.0.0.10:5432/mapkeeper_test
